
import 'package:flutter/material.dart';
import 'package:mattermost_flutter/types/typing.dart';

class MyConvertedComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is a converted component'),
    );
  }
}
